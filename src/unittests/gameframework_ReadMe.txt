This directory contains all the unit tests for the core game library classes.

Specific game inplementations such as ping pong, flight control, etc. will have their own unit tests inside them.  This directory is only a test directory for commmon files.

To build and run on the command line type:

nmake unittests
